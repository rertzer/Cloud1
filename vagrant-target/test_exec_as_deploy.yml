- name: "I am deploy"
  hosts: all
  remote_user: deploy
  vars:
    ansible_ssh_private_key_file: deploy_ed25519
  tasks:
    - name: "test executed as deploy"
      debug:
        msg: "I am {{ ansible_user_id }}"
